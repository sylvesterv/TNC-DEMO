---
- name: Playbook 6 - Start Services on all Controllers
  hosts: controller
  user: heat-admin
  become: true
  tasks:
    - set_fact:
        bootstrap_node: false 
    - name: test ping
      ping:
